Place your application logo image here with the filename `emolens_logo.jpeg`.

This file is referenced by `pubspec.yaml` for `flutter_launcher_icons` and by the
app's splash screen at `assets/image/emolens_logo.jpeg`.

To generate platform launcher icons after adding the image, run:

```
flutter pub get
flutter pub run flutter_launcher_icons:main
```

Replace this README with your real image file (binary JPEG/PNG).
